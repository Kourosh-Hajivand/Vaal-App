# راهنمای تست نمایش دانلود محتوا

این راهنما روش‌های مختلف تست کردن نمایش دانلود محتوا را توضیح می‌دهد.

## روش‌های تست

### 1️⃣ تست با پاک کردن Cache (ساده‌ترین روش)

#### روش A: استفاده از دکمه Debug (در Development Mode)
1. اپ را در حالت Development اجرا کنید (`__DEV__ = true`)
2. در صفحه Advertisement، یک Debug Overlay در گوشه بالا سمت چپ نمایش داده می‌شود
3. روی دکمه **"🗑️ پاک کردن Cache"** کلیک کنید
4. تایید کنید
5. اپ را refresh کنید (یا دوباره باز کنید)
6. حالا باید صفحه Loading با نمایش دانلود را ببینید

#### روش B: استفاده از کد (در Console یا DevTools)
```javascript
// در React Native Debugger یا Console
import { cacheManager } from './src/utils/cache/cacheManager';
await cacheManager.clearCache();
// سپس اپ را refresh کنید
```

#### روش C: پاک کردن دستی Cache
```bash
# برای Android
adb shell run-as com.yourapp
rm -rf /data/data/com.yourapp/files/media-cache

# برای iOS (نیاز به Simulator یا Device با دسترسی)
# مسیر: Documents/media-cache
```

### 2️⃣ تست با قطع/وصل کردن اینترنت

1. **مرحله 1**: اپ را باز کنید و بگذارید محتوا دانلود شود
2. **مرحله 2**: اینترنت را قطع کنید (Airplane Mode یا WiFi خاموش)
3. **مرحله 3**: Cache را پاک کنید (روش 1)
4. **مرحله 4**: اینترنت را وصل کنید
5. **مرحله 5**: اپ را refresh کنید
6. حالا باید صفحه Loading با نمایش دانلود را ببینید

### 3️⃣ تست با تغییر Manifest (تست واقعی)

1. در Backend، یک محتوای جدید به playlist اضافه کنید
2. یا `updated_at` یک محتوای موجود را تغییر دهید
3. اپ را refresh کنید
4. محتوای جدید/تغییر یافته باید دانلود شود

### 4️⃣ تست با Simulate Slow Network

#### در React Native Debugger:
```javascript
// Simulate slow network
Network.setNetworkConditions({
  downloadThroughput: 50 * 1024, // 50 KB/s
  uploadThroughput: 50 * 1024,
  latency: 500
});
```

#### در Chrome DevTools (برای Web):
1. DevTools را باز کنید (F12)
2. به تب Network بروید
3. روی "Throttling" کلیک کنید
4. "Slow 3G" یا "Fast 3G" را انتخاب کنید

### 5️⃣ تست با اضافه کردن محتوای بزرگ

1. یک ویدیو بزرگ (مثلاً 50+ MB) به playlist اضافه کنید
2. Cache را پاک کنید
3. اپ را refresh کنید
4. حالا باید progress bar را به صورت smooth ببینید

## چیزهایی که باید بررسی کنید

### ✅ صفحه Loading (وقتی هیچ محتوایی آماده نیست)
- [ ] نمایش "در حال دانلود محتوا..."
- [ ] نمایش تعداد آماده شده (مثلاً "2 از 5 آماده")
- [ ] نمایش Progress Bar کلی
- [ ] نمایش درصد کلی
- [ ] نمایش نام آیتم در حال دانلود
- [ ] نمایش نوع آیتم (ویدیو 📹 یا عکس 🖼️)
- [ ] نمایش Progress Bar برای آیتم فعلی
- [ ] نمایش درصد دانلود آیتم فعلی

### ✅ Overlay دانلود (وقتی ویدیو در حال پخش است)
- [ ] نمایش Overlay در پایین صفحه
- [ ] نمایش "در حال دانلود محتوا"
- [ ] نمایش نام آیتم در حال دانلود
- [ ] نمایش Progress Bar
- [ ] نمایش درصد دانلود
- [ ] به‌روزرسانی smooth درصد (بدون lag)

### ✅ Debug Overlay (در Development Mode)
- [ ] نمایش تعداد Ready/Total
- [ ] نمایش وضعیت دانلود
- [ ] نمایش نام و درصد آیتم در حال دانلود
- [ ] دکمه "پاک کردن Cache" کار می‌کند

## سناریوهای تست

### سناریو 1: دانلود اولیه
1. Cache را پاک کنید
2. اپ را باز کنید
3. باید صفحه Loading با نمایش دانلود را ببینید
4. وقتی اولین ویدیو دانلود شد، باید پخش شود
5. در حالی که ویدیو پخش می‌شود، Overlay دانلود باید نمایش داده شود

### سناریو 2: دانلود Progressive
1. Cache را پاک کنید
2. اپ را باز کنید
3. باید ببینید که ویدیوها یکی یکی دانلود می‌شوند
4. هر ویدیو که دانلود شد، باید به playlist اضافه شود
5. Progress باید به صورت real-time به‌روزرسانی شود

### سناریو 3: دانلود در پس‌زمینه
1. بگذارید یک ویدیو پخش شود
2. Cache را پاک کنید (یا محتوای جدید اضافه کنید)
3. باید Overlay دانلود در پایین صفحه نمایش داده شود
4. ویدیو باید بدون وقفه پخش شود
5. وقتی دانلود تمام شد، Overlay باید ناپدید شود

### سناریو 4: خطا در دانلود
1. اینترنت را قطع کنید
2. Cache را پاک کنید
3. اپ را باز کنید
4. باید پیام خطا نمایش داده شود
5. اینترنت را وصل کنید
6. باید retry خودکار انجام شود (هر 10 ثانیه)

## نکات مهم

1. **Progress Update**: Progress باید هر 500ms به‌روزرسانی شود (طبق `progressInterval` در `cacheManager`)
2. **State Management**: وقتی یک آیتم دانلود شد، باید از `downloading` به `ready` تغییر کند
3. **Memory**: Progress برای آیتم‌های دانلود شده باید پاک شود (یا به 100% تنظیم شود)
4. **Performance**: Progress update نباید باعث lag در پخش ویدیو شود

## Troubleshooting

### مشکل: Progress نمایش داده نمی‌شود
- بررسی کنید که `onProgress` callback به `cacheFile` پاس داده شده است
- بررسی کنید که `downloadProgress` state به‌روزرسانی می‌شود
- Console را چک کنید برای خطاها

### مشکل: Overlay نمایش داده نمی‌شود
- بررسی کنید که `contentItems.length > readyItems.length`
- بررسی کنید که حداقل یک آیتم با status `downloading` وجود دارد
- بررسی کنید که `currentDownloadingItem` null نیست

### مشکل: Progress به 100% نمی‌رسد
- بررسی کنید که `setDownloadProgress` با value 100 بعد از دانلود کامل صدا زده می‌شود
- بررسی کنید که `onProgress` callback درست کار می‌کند

## کدهای مفید برای تست

```javascript
// در React Native Debugger Console

// مشاهده وضعیت دانلود
console.log('Download Status:', downloadStatus);
console.log('Download Progress:', downloadProgress);
console.log('Local Paths:', localPaths);

// پاک کردن cache
import { cacheManager } from './src/utils/cache/cacheManager';
await cacheManager.clearCache();

// مشاهده metadata cache
const metadata = await AsyncStorage.getItem('@media_cache_metadata');
console.log('Cache Metadata:', JSON.parse(metadata));
```
